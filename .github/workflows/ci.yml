name: CI/CD

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

env:
  DJANGO_SECRET_KEY: ci-secret-key-not-for-production
  DJANGO_DEBUG: "true"
  CELERY_TASK_ALWAYS_EAGER: "True"
  PYTHON_VERSION: "3.11"

jobs:
  test:
    name: Test (Python 3.11)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Django system check
        run: |
          python manage.py check

      - name: Run tests
        run: |
          python manage.py test --noinput -v2

      - name: Compile messages
        run: |
          python manage.py compilemessages --noinput 2>/dev/null || echo "gettext not available, skipping"

  docker:
    name: Docker Build & Push
    runs-on: ubuntu-latest
    needs: test
    # プッシュ時のみビルド（PRではビルドのみ）
    if: github.event_name == 'push' || startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=sha,prefix=sha-

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # 本番デプロイ（Fly.io / Render / Railway 等へのデプロイを自由に追加可能）
  # deploy:
  #   name: Deploy
  #   runs-on: ubuntu-latest
  #   needs: docker
  #   if: startsWith(github.ref, 'refs/tags/')
  #   steps:
  #     - name: Deploy to production
  #       run: echo "デプロイ先の設定をここに追加 (Fly.io / Render / Railway / VPS)"
